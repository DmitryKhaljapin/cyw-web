import { Component } from '@angular/core';
import { Button } from '../../commin-ui/button/button';

@Component({
  selector: 'app-auth',
  imports: [Button],
  templateUrl: './auth.html',
  styleUrl: './auth.css',
})
export class Auth {}
