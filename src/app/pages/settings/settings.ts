import { Component } from '@angular/core';
import { Button } from '../../commin-ui/button/button';

@Component({
  selector: 'app-settings',
  imports: [Button],
  templateUrl: './settings.html',
  styleUrl: './settings.css',
})
export class Settings {}
